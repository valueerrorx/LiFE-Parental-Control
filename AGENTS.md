# Agent instructions — LiFE Parental Control

- **Cursor:** Follow `.cursor/rules/` (especially `graphify.mdc` when `graphify-out/` exists).
- **Claude Code / CLI:** Same Graphify policy is documented in **`CLAUDE.md`** (graphify section).

When `graphify-out/GRAPH_REPORT.md` exists, use it for architecture and navigation before large exploratory searches. Rebuild the graph after structural code changes using the command in `CLAUDE.md` or `.cursor/rules/graphify.mdc`.
