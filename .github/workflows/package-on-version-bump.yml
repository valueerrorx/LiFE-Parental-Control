# Builds AppImage + deb and publishes a GitHub Release when package.json "version" changes on main.

name: Package on version bump

on:
  push:
    branches:
      - main
    paths:
      - 'package.json'

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check-version:
    runs-on: ubuntu-latest
    outputs:
      build: ${{ steps.diff.outputs.build }}
      version: ${{ steps.diff.outputs.version }}
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - id: diff
        run: |
          set -euo pipefail
          CURR=$(node -p "require('./package.json').version")
          echo "version=$CURR" >> "$GITHUB_OUTPUT"
          BEFORE="${{ github.event.before }}"
          if [ "$BEFORE" = "0000000000000000000000000000000000000000" ]; then
            echo "build=true" >> "$GITHUB_OUTPUT"
            echo "No parent SHA; treating as first push — building."
            exit 0
          fi
          if ! git cat-file -e "$BEFORE^{commit}" 2>/dev/null; then
            echo "build=true" >> "$GITHUB_OUTPUT"
            echo "Parent commit not in history; building."
            exit 0
          fi
          PREV=$(git show "$BEFORE:package.json" 2>/dev/null | jq -r '.version // empty' 2>/dev/null || true)
          if [ -z "$PREV" ]; then
            echo "build=true" >> "$GITHUB_OUTPUT"
            echo "Could not read previous version; building."
            exit 0
          fi
          if [ "$PREV" != "$CURR" ]; then
            echo "build=true" >> "$GITHUB_OUTPUT"
            echo "Version changed: $PREV -> $CURR"
          else
            echo "build=false" >> "$GITHUB_OUTPUT"
            echo "package.json changed but version is still $CURR — skipping build."
          fi

  linux:
    needs: check-version
    if: needs.check-version.outputs.build == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: Changelog
        run: |
          set -euo pipefail
          BEFORE="${{ github.event.before }}"
          if [ "$BEFORE" = "0000000000000000000000000000000000000000" ]; then
            NOTES=$(git log --no-merges -n 40 --pretty=format:'- %s (%h)' HEAD)
          else
            NOTES=$(git log --no-merges --pretty=format:'- %s (%h)' "$BEFORE..HEAD" || true)
          fi
          if [ -z "$NOTES" ]; then
            NOTES="- (no commits in range)"
          fi
          {
            echo "## Changes"
            echo ""
            echo "$NOTES"
          } > release-notes.md
      - uses: actions/setup-node@v6
        with:
          node-version: '24'
          cache: npm
      - name: Install AppImage update tools
        run: sudo apt-get update && sudo apt-get install -y zsync binutils
      - run: npm ci
      - run: npm run build
        env:
          ELECTRON_BUILDER_PUBLISH: never
      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          shopt -s nullglob
          TAG="v${{ needs.check-version.outputs.version }}"
          NAME="LiFE Parental Control ${{ needs.check-version.outputs.version }}"
          echo "Creating release ${TAG}"
          # (fail fast if no assets matched)
          ASSET_FILES=(dist/*.AppImage dist/*.AppImage.zsync dist/*.deb)
          if [ "${#ASSET_FILES[@]}" -eq 0 ]; then
            echo "No release assets found in dist/"
            exit 1
          fi
          gh release create "$TAG" "${ASSET_FILES[@]}" \
            --title "$NAME" \
            --notes-file release-notes.md
      - uses: actions/upload-artifact@v6
        with:
          name: linux-dist
          path: dist/*
